<Page x:Class="kanzeed.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:kanzeed.Pages"
      Title="Корзина">

    <Grid Background="#F8F9FA">
        <Border Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Ваша корзина" FontSize="18" FontWeight="Bold"/>

                <ListView x:Name="CartListView" Grid.Row="1" Margin="0,10,0,10">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding ProductName}" Grid.Column="0" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="−" Width="28" Click="DecreaseQty_Click" Tag="{Binding ProductId}"/>
                                    <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center" VerticalAlignment="Center"/>
                                    <Button Content="+" Width="28" Click="IncreaseQty_Click" Tag="{Binding ProductId}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding LineTotal, StringFormat={}{0:C}}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <Button Content="Удалить" Click="RemoveItem_Click" Tag="{Binding ProductId}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
                    <TextBlock x:Name="TotalAmountText" FontSize="14" VerticalAlignment="Center"/>
                    <Button Content="Оформить заказ" Click="CheckoutButton_Click"/>
                    <Button Content="Назад" Click="BackButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
